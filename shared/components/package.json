{
    "name": "@shared/components",
    "version": "0.0.1",
    "private": true,
    "main": "./src/index.ts",
    "scripts": {
        "test": "echo test",
        "commit:all": "npm run test && git add --all && git-cz"
    },
    "lint-staged": {
        "src/**/*.{ts,vue}": [
            "npm run prettier",
            "npm run lint"
        ]
    },
    "eslintIgnore": [
        "*.js",
        "*.d.ts"
    ]
}